# ap-clerk-cli

Thin CLI frontend for accounts payable invoice processing.

## Usage

From the repo root:

```bash
uv run --package ap-clerk-cli ap-clerk --help
uv run --package ap-clerk-cli ap-clerk extract <invoice>
```
